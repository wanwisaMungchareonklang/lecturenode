const calSquareArea = (widths, longs) => {
    return widths * longs
}

const calTriangleArea = (bases, highs) => {
    return (bases * highs) / 2
}

const calCircleArea = (radius) => {
    return Math.PI * radius * radius
}

export { calSquareArea, calTriangleArea, calCircleArea }


